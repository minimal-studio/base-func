---
name: Number / 数字处理
route: /number
---

import { useState } from 'react';
import { Playground } from 'docz'
import { GetFloatLen, SetFloatLen, ToFixed, ToggleBasicFloatLen } from '../src/number';

# Number

basic-helper 提供统一处理 js 的数字、浮点数的方式，默认长度为小数点后 4 位，主要作用于

- ToFixed
- MoneyFormat

## 获取基准浮点位数

```js
import { GetFloatLen } from '../src/number';

GetFloatLen();
```

## 设置基准浮点位数

```js
import { SetFloatLen } from '../src/number';

let len = 1;

SetFloatLen(len);
```

## 数字剪切和浮点数开关

```js
import { ToFixed, ToggleBasicFloatLen } from '../src/number';

let res = 2 / 3;

ToFixed(res);
ToggleBasicFloatLen();
```

<Playground>
  {
    () => {
      const initNumb = 2 / 3;
      const [numb, setNumb] = useState(initNumb);
      const [isDisplayFloat, setFloat] = useState(true);
      return (
        <div>
          <div>数字: {numb}</div>
          <hr />
          <span className="btn theme mr10" onClick={e => {
            setNumb(ToFixed(numb))
          }}>使用 ToFixed 剪切</span>
          <span className="btn red mr10" onClick={e => {
            SetFloatLen(2)
          }}>设置为两位小数</span>
          <span className="btn green mr10" onClick={e => {
            setNumb(initNumb)
            setFloat(ToggleBasicFloatLen())
          }}>浮点小数{isDisplayFloat ? '开' : '关'}</span>
          <span className="btn gold mr10" onClick={e => {
            setNumb(initNumb)
            setFloat(4)
          }}>重置</span>
        </div>
      )
    }
  }
</Playground>
